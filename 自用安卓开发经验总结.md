# Operit Android 开发全书

> 实战血泪汇总：Shizuku · 无障碍 · 保活 · 定位 · 权限 · 部署
> 最后更新：2026-05-11（v1.6.0，ActionPilot 录制引擎整合 + create_note）

---

## 目录

- [一、Shizuku 核心经验](#一shizuku-核心经验)
- [二、无障碍服务（AccessibilityService）](#二无障碍服务accessibilityservice)
- [三、应用保活与开机自启](#三应用保活与开机自启)
- [四、获取已安装应用列表](#四获取已安装应用列表)
- [五、Android 权限体系](#五android-权限体系)
- [六、模拟定位开发](#六模拟定位开发)
- [七、GitHub 上传流程](#七github-上传流程)
- [八、UI自动化与AutoJS6](#八ui自动化与autojs6)
- [九、Android 项目构建](#九android-项目构建)
- [十、ActionPilot 项目架构](#十actionpilot-项目架构)
- [十一、万能APP疑难杂症](#十一万能app疑难杂症)
- [十二、通用经验教训](#十二通用经验教训)
- [十三、AI 工具架构（来自 Operit）](#十三ai-工具架构来自-operit)
- [十四、Shizuku AI 开发经验](#十四shizuku-ai-开发经验)
  - [14.6 核心原则：所有功能必须工具化](#146-核心原则所有功能必须工具化)
  - [14.7 UI 操作录制（UiMapRecorder + read_uimap）](#147-ui-操作录制uimaprecorder--read_uimap)
  - [14.8 关键修复：AccessibilityWindowInfo 替代 rootInActiveWindow](#148-关键修复accessibilitywindowinfo-替代-rootinactivewindow)

---

# 一、Shizuku 核心经验

## 1.1 核心理念

> **Shizuku 的本质是给 APP 提权到 ADB shell 级别。既然有了 shell 权限，直接执行 shell 命令就是最正确的方式。**

不需要 Binder、不需要 AIDL、不需要反射调用 Shizuku 内部方法——直接通过 `newProcess("sh")` 获取 Shell 进程，写入命令即可。

## 1.2 ShizukuShell.java 最终实现（Java 反射）

```java
public class ShizukuShell {
    private static Method newProcessMethod;
    private static Class<?> shizukuClass;

    public static boolean isAvailable() {
        try {
            if (shizukuClass == null)
                shizukuClass = Class.forName("rikka.shizuku.Shizuku");
            Method ping = shizukuClass.getMethod("ping");
            return (boolean) ping.invoke(null);
        } catch (Exception e) { return false; }
    }

    public static boolean isGranted() {
        try {
            Class<?> c = Class.forName("rikka.shizuku.Shizuku");
            int r = (int) c.getMethod("checkSelfPermission").invoke(null);
            return r == 0;
        } catch (Exception e) { return false; }
    }

    public static void requestPermission(int requestCode) {
        try {
            Class<?> c = Class.forName("rikka.shizuku.Shizuku");
            c.getMethod("requestPermission", int.class).invoke(null, requestCode);
        } catch (Exception ignored) {}
    }

    public static ShellResult exec(String cmd) {
        try {
            Class<?> c = Class.forName("rikka.shizuku.Shizuku");
            Method m = c.getDeclaredMethod("newProcess",
                String[].class, String[].class, String.class);
            m.setAccessible(true);
            Process proc = (Process) m.invoke(null,
                new String[]{"sh"}, null, null);
            // 写入命令 + 退出码标记
            java.io.OutputStream os = proc.getOutputStream();
            os.write((cmd + " 2>&1\necho \"__EXIT__:$?\"\n").getBytes());
            os.write("exit\n".getBytes());
            os.flush();
            // 读取输出
            java.io.BufferedReader reader = new java.io.BufferedReader(
                new java.io.InputStreamReader(proc.getInputStream()));
            StringBuilder sb = new StringBuilder();
            String line;
            while ((line = reader.readLine()) != null) sb.append(line).append("\n");
            String output = sb.toString().trim();
            // 提取退出码
            int ec = proc.waitFor();
            java.util.regex.Matcher mat = java.util.regex.Pattern
                .compile("__EXIT__:(\\d+)").matcher(output);
            if (mat.find()) {
                try { ec = Integer.parseInt(mat.group(1)); } catch (Exception ignored) {}
            }
            String clean = output.replaceAll("__EXIT__:\\d+\\s*", "").trim();
            return new ShellResult(ec, clean);
        } catch (Exception e) {
            return new ShellResult(-1, "ERR: " + e.getMessage());
        }
    }

    public static class ShellResult {
        public final int exitCode;
        public final String output;
        public ShellResult(int ec, String out) { this.exitCode = ec; this.output = out; }
    }
}
```

### ⚠️ 反射参数传递陷阱

```java
// ✅ 正确写法（直接传参）
Process proc = (Process) m.invoke(null, new String[]{"sh"}, null, null);

// ❌ 错误写法（多包一层数组不会报错，但命令不执行）
Process proc = (Process) m.invoke(null, new Object[]{new String[]{"sh"}, null, null});
```

`Method.invoke(Object obj, Object... args)` 是**可变参数**，多包一层 `new Object[]{}` 是否真的会导致参数错乱取决于 Java 版本——在某些版本上 invoke 会解包数组，使得 `new Object[]{new String[]{"sh"}, null, null}` 实际上等价于分开传参。但为了保证一致性，**直接用分开传参的写法**。

### 1.3 反射类必须放 Java 文件

Kotlin 编译检查会拦截对 `private` 方法的反射调用，而 Java 不检查。所以 `ShizukuShell.java` 必须是 Java 文件。

### 1.4 管道写入模式

获取 Shell 后不要直接把参数传为数组，而是通过 shell 进程的 `getOutputStream()` 写入完整命令：

```java
Process proc = (Process) m.invoke(null, new String[]{"sh"}, null, null);
if (proc != null) {
    PrintWriter writer = new PrintWriter(proc.getOutputStream());
    writer.println(cmd);        // 写入完整命令
    writer.println("echo __EXIT__:$?");  // 标记退出码
    writer.flush();
    writer.close();
    // 读取 stdout ...
}
```

### 1.5 常用命令集

**权限相关：**
```bash
appops set <pkg> android:mock_location allow
pm grant <pkg> android.permission.ACCESS_FINE_LOCATION
pm grant <pkg> android.permission.POST_NOTIFICATIONS
appops set <pkg> android:system_alert_window allow
```

**APK 安装：**
```bash
# 推荐：从 /data/local/tmp/ 安装
adb push app.apk /data/local/tmp/
adb shell pm install -r /data/local/tmp/app.apk

# 从 sdcard 安装（SELinux 可能拦截）
cat /sdcard/Download/app.apk | pm install -g -S $(stat -c%s /sdcard/Download/app.apk) -r
```

**应用控制：**
```bash
am start -n <pkg>/<pkg>.AdbCommandActivity --es action run
am start -a android.intent.action.VIEW -d "https://www.baidu.com/s?wd=关键词"
dumpsys window | grep mCurrentFocus
```

### 1.6 自动授权时机

```kotlin
// ❌ 错误：Activity.onCreate 中执行（Shizuku Binder 未就绪）
override fun onCreate() {
    ShizukuShell.exec("appops set ...")  // 太早了！
}

// ✅ 正确：Service.onStartCommand 中执行（此时 Shizuku 已就绪）
override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
    ShizukuShell.exec("appops set $packageName android:mock_location allow")
}
```

**关键**：不检查 `isAvailable`/`isGranted`，直接 try-catch 执行。检查反而可能因时序问题误判。

### 1.7 settings put secure 的 SELinux 差异

**重要发现**：同一个 `settings put secure` 命令在不同上下文表现不同：

| 执行环境 | vivo 设备上结果 |
|---------|--------------|
| `adb shell settings put secure` | ❌ 被 vivo 拦截，写入无效 |
| `ShizukuShell.exec("settings put secure ...")` | ✅ 成功 |

**根因**：vivo 设备的 SELinux 策略对 ADB shell 和 Shizuku 进程的约束不同。Shizuku (ADB mode) 的 `newProcess("sh")` 创建的进程有更宽松的安全上下文。

**实践**：不依赖 ADB 调试，直接在 App 内通过 ShizukuShell 执行：
```java
ShizukuShell.exec("settings put secure enabled_accessibility_services \"" + component + "\"");
ShizukuShell.exec("settings put secure accessibility_enabled 1");
```

### 1.8 依赖配置

```kotlin
dependencies {
    implementation("dev.rikka.shizuku:api:13.1.5")
    implementation("dev.rikka.shizuku:provider:13.1.5")
}
```

- minSdk **必须 >= 24**
- `ShizukuProvider` 必须在 AndroidManifest 中声明

---

# 二、无障碍服务（AccessibilityService）

## 2.1 核心机制

```
用户手动开启无障碍
      ↓
系统写入 settings secure:
  enabled_accessibility_services =  com.example/...Service
      ↓
手机重启 → SystemServer → AccessibilityManagerService 读取配置
      ↓
检查 android:enabled=true → 确认可用
检查 android:directBootAware=true → 无需等用户解锁
      ↓
系统自动创建 Service 实例 → onServiceConnected() 回调
```

**核心优势**：不写一行自启代码，全靠系统级保活。

## 2.2 AndroidManifest 声明（完整正确版）

```xml
<service
    android:name=".YourAccessibilityService"
    android:exported="true"
    android:enabled="true"
    android:directBootAware="true"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

| 属性 | 作用 |
|------|------|
| `BIND_ACCESSIBILITY_SERVICE` | **必须**，确保只有系统能绑定该服务 |
| `exported="true"` | **必须**，system_server 跨进程绑定需要 |
| `enabled="true"` | 明确告知系统该组件已启用 |
| `directBootAware="true"` | 允许在解锁前（Direct Boot 模式）运行 |

### ⚠️ 踩坑：无障碍服务不显示在设置列表

**根因**：缺少 `BIND_ACCESSIBILITY_SERVICE` 权限 + `exported="false"`。

系统 `AccessibilityManagerService` 运行在 `system_server` 进程，必须跨进程绑定。如果服务没有声明 `BIND_ACCESSIBILITY_SERVICE`，系统拒绝绑定；如果 `exported="false"`，跨进程不可见。

**参考来源**：PermOpener 项目的 AndroidManifest 配置（已验证可工作）。

## 2.3 accessibility_service_config.xml

```xml
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100" />
```

**关键 Flag**：

| Flag | 说明 |
|------|------|
| `canRetrieveWindowContent="true"` | **必须**，检索窗口内容总开关 |
| `flagReportViewIds` | 让节点携带 viewId 信息 |
| `flagRetrieveInteractiveWindows` | 访问所有交互窗口 |
| `typeAllMask` | 监听所有事件类型（方便开发，上线建议精简）|

## 2.4 Service 实现要点

```kotlin
class YourAccessibilityService : AccessibilityService() {

    override fun onServiceConnected() {
        super.onServiceConnected()
        // 动态设置 ServiceInfo
        val info = AccessibilityServiceInfo().apply {
            eventTypes = AccessibilityEvent.TYPE_VIEW_CLICKED or
                    AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED
            feedbackType = AccessibilityServiceInfo.FEEDBACK_GENERIC
            flags = AccessibilityServiceInfo.FLAG_REPORT_VIEW_IDS
            notificationTimeout = 100
        }
        this.serviceInfo = info
    }

    override fun onAccessibilityEvent(event: AccessibilityEvent?) {
        if (event == null || !isRecording) return  // 用户手动控制开关
        when (event.eventType) {
            AccessibilityEvent.TYPE_VIEW_CLICKED -> {
                val (label, viewId) = extractClickInfo(event)
                // 记录点击
            }
            AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED -> {
                val pkg = event.packageName?.toString()
                val screen = event.className?.toString()
                // 记录页面切换
            }
        }
        event.source?.recycle()  // 必须回收！
    }

    override fun onInterrupt() {}
}
```

### 5 级 Label 提取策略

```kotlin
private fun extractBestLabel(node: AccessibilityNodeInfo): String {
    // 1. contentDescription（TalkBack 主标签）
    node.contentDescription?.toString()?.takeIf { it.isNotBlank() }?.let { return it }
    // 2. text（按钮可见文字）
    node.text?.toString()?.takeIf { it.isNotBlank() }?.let { return it }
    // 3. hintText（输入框提示）
    node.hintText?.toString()?.takeIf { it.isNotBlank() }?.let { return it }
    // 4. viewId 资源名（最后一段）
    node.viewIdResourceName?.let { return it.substringAfterLast('/') }
    // 5. 子节点递归
    for (i in 0 until node.childCount) {
        node.getChild(i)?.let { child ->
            child.text?.toString()?.takeIf { it.isNotBlank() }?.let {
                child.recycle(); return it
            }
            child.recycle()
        }
    }
    return ""
}
```

## 2.5 安全编程原则

### ⚠️ 血泪教训：手机变砖的完整链路

```
手机开机
  ↓
系统自动启动无障碍服务（用户之前开启了）
  ↓
onAccessibilityEvent 被高频调用（开机过程系统UI变化极多）
  ↓
监听 TYPE_WINDOW_CONTENT_CHANGED → 递归遍历 UI 树
  ↓
叠加悬浮窗覆盖层 → 用户触摸屏幕无响应
  ↓
重启 → 再次进入死循环 → 只能 ADB 线连电脑解决
```

### 安全守则

```kotlin
// ❌ 不要在 onServiceConnected 中自动开启任何功能！
override fun onServiceConnected() {
    // enableOverlay() // ← 千万不要！
}

// ✅ 所有功能由用户手动触发
override fun onAccessibilityEvent(event: AccessibilityEvent?) {
    if (event == null || !recording) return  // ← recording 必须用户手动开启
}

// ❌ 不要监听高频事件做耗时操作 (TYPE_WINDOW_CONTENT_CHANGED)
// ✅ 只用低频事件 (TYPE_WINDOW_STATE_CHANGED, TYPE_VIEW_CLICKED)
// ❌ 不要用 START_STICKY（被杀后自动重启，危险！）
// ✅ 用 START_NOT_STICKY
```

### ADB 急救命令

```bash
# 清空无障碍服务列表（最有效）
adb shell settings put secure enabled_accessibility_services ""
# 停用无障碍功能
adb shell settings put secure accessibility_enabled 0
# 强制卸载
adb uninstall com.example.app
# 安全模式启动（第三方无障碍服务不启动）
adb reboot safe-mode
```

## 2.6 Android 14 Restricted Settings 绕过

**问题**：Android 14 限制侧载 App（targetSdk>=33）启用无障碍服务。

**解决方法**：

1. **targetSdk 降级到 32**（最有效）：
```kotlin
defaultConfig { targetSdk = 32 }
```

2. **安装时绕过 targetSdk 检查**：
```bash
adb push app.apk /data/local/tmp/
adb shell pm install -r /data/local/tmp/app.apk
```

**原理**：Restricted Settings 策略只对 targetSdk>=33 的应用生效。targetSdk=32 不受限制。

---

# 三、应用保活与开机自启

## 3.1 无障碍服务自启（核心方案）

**不需要 BOOT_COMPLETED 广播**。系统 `AccessibilityManagerService` 在开机时自动读取 `settings_secure.xml`，恢复已授权的无障碍服务。这是最可靠的自启方式。

## 3.2 前台服务保活

在 `onServiceConnected()` 中启动前台服务，常驻通知栏，显著降低被回收概率：

```kotlin
override fun onServiceConnected() {
    super.onServiceConnected()
    startForegroundService()
    // ... 业务逻辑
}

private fun startForegroundService() {
    val intent = Intent(this, KeepAliveService::class.java)
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
        startForegroundService(intent)
    } else {
        startService(intent)
    }
}

class KeepAliveService : Service() {
    override fun onCreate() {
        super.onCreate()
        val notification = createNotification()
        startForeground(NOTIFICATION_ID, notification)
    }
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        return START_NOT_STICKY  // 安全
    }
}
```

## 3.3 电池优化白名单

```xml
<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
```

```kotlin
fun requestIgnoreBatteryOptimizations(context: Context) {
    val intent = Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS).apply {
        data = Uri.parse("package:${context.packageName}")
    }
    context.startActivity(intent)
}
```

## 3.4 WorkManager 心跳自检

```kotlin
class KeepAliveWorker(context: Context, params: WorkerParameters) : Worker(context, params) {
    override fun doWork(): Result {
        if (!isAccessibilityServiceRunning()) {
            showServiceDownNotification()  // 引导用户恢复
        }
        return Result.success()
    }
    private fun isAccessibilityServiceRunning(): Boolean {
        val am = applicationContext.getSystemService(Context.ACCESSIBILITY_SERVICE) as AccessibilityManager
        val enabledServices = am.getEnabledAccessibilityServiceList(AccessibilityServiceInfo.FEEDBACK_ALL_MASK)
        val serviceId = "${applicationContext.packageName}/.YourAccessibilityService"
        return enabledServices.any { it.id == serviceId }
    }
}

// 设置周期性检查（每小时）
val request = PeriodicWorkRequestBuilder<KeepAliveWorker>(Duration.ofHours(1)).build()
WorkManager.getInstance(context).enqueueUniquePeriodicWork("keep_alive", KEEP, request)
```

## 3.5 各类场景成活率对比

| 场景 | 纯无障碍方案 | 改进方案 |
|------|-------------|---------|
| 正常开机 | ✅ 内核级自启 | ✅ |
| 系统回收 | ✅ 自动复活 | ✅ 双层守护 |
| 一键清理后台 | ✅ 大概率存活 | ✅ 存活率极高 |
| 强制停止 | ❌ 彻底死亡 | ⚠️ 通知引导 |
| 用户关闭开关 | ❌ 彻底死亡 | ⚠️ 通知引导 |
| Android 15 深杀 | ⚠️ 可能失效 | ✅ 防御检测 |
| 断电重启 | ✅ 自动复活 | ✅ 自动复活 |

---

# 四、获取已安装应用列表

## 4.1 问题背景

Android 11（API 30）引入**包可见性限制**（Package Visibility）。第三方应用调用 `PackageManager.getInstalledApplications()` 时，如果不声明 `QUERY_ALL_PACKAGES`，系统只返回调用者自身的包信息。

## 4.2 不同方法对比

| 方法 | 结果 | 原因 |
|------|------|------|
| `queryIntentActivities(CATEGORY_LAUNCHER)` | ~20个 | 只返回桌面有图标的 |
| `pm list packages -3` (adb shell) | 104个 | shell UID，无限制 |
| `pm list packages -3` (run-as app) | 3个 | app UID，包可见性限制 |
| `getInstalledApplications()` (无权限) | 很少 | 受 QUERY_ALL_PACKAGES 限制 |
| `getInstalledApplications()` (QUERY_ALL_PACKAGES) | 全部 | 正常返回 |

## 4.3 最终方案（2026-05-11 更新）

**Shizuku 优先 + PackageManager 回退**。利用 Shizuku 执行 `cmd package list packages` 获取**全部**包名（含系统应用），再逐包用 `getApplicationInfo` 解析应用名：

```kotlin
private fun loadInstalledApps(context: Context): List<AppInfo> {
    val pm = context.packageManager
    val allApps = mutableMapOf<String, String>()

    // Method 1: Shizuku cmd package list（无权限弹窗）
    try {
        val cmdPkg = ShizukuShell.exec("cmd package list packages")  // 不加 -3，列出全部
        val lines = cmdPkg.lines()
            .map { it.trim() }.filter { it.startsWith("package:") }
            .map { it.removePrefix("package:") }.filter { it.isNotBlank() }
        for (pkg in lines) {
            val label = try {
                val info = pm.getApplicationInfo(pkg, 0)
                info.loadLabel(pm).toString()
            } catch (_: Exception) { pkg }
            val isSystem = try {
                (pm.getApplicationInfo(pkg, 0).flags and android.content.pm.ApplicationInfo.FLAG_SYSTEM) != 0
            } catch (_: Exception) { false }
            val prefix = if (isSystem) "[系统] " else ""
            allApps[pkg] = "$prefix$label"
        }
    } catch (_: Exception) {}

    // Method 2: PackageManager fallback（仅 Shizuku 失败时）
    if (allApps.isEmpty()) {
        // getInstalledApplications + QUERY_ALL_PACKAGES
    }

    return allApps.entries.map { AppInfo(it.key, it.value) }.sortedBy { it.label }
}
```

**原理**：`getApplicationInfo(指定包名, 0)` 查单个包不需要 QUERY_ALL_PACKAGES 权限。包列表从 Shizuku 拿，逐个解析应用名，完全绕过权限弹窗。系统应用标 `[系统]` 前缀，方便区分。

**注意**：vivo 原子笔记包名为 `com.android.notes`，是系统应用，用 `pm list packages -3` 不会出现，必须用全量模式。

---

# 五、Android 权限体系

## 5.1 pm grant（危险权限）

```bash
# 定位
pm grant <pkg> android.permission.ACCESS_FINE_LOCATION
pm grant <pkg> android.permission.ACCESS_COARSE_LOCATION
pm grant <pkg> android.permission.ACCESS_BACKGROUND_LOCATION

# 其他
pm grant <pkg> android.permission.CAMERA
pm grant <pkg> android.permission.RECORD_AUDIO
pm grant <pkg> android.permission.READ_PHONE_STATE
pm grant <pkg> android.permission.SEND_SMS
pm grant <pkg> android.permission.READ_CONTACTS
pm grant <pkg> android.permission.POST_NOTIFICATIONS  # Android 13+
```

## 5.2 appops（操作权限）

```bash
# 标准格式（带 android: 前缀）
appops set <pkg> android:mock_location allow
appops set <pkg> android:system_alert_window allow
appops set <pkg> android:post_notifications allow
appops set <pkg> android:manage_external_storage allow

# 厂商专用（不带 android: 前缀）
appops set <pkg> PROJECT_MEDIA allow
```

## 5.3 settings put secure（系统设置）

```bash
# 启用无障碍服务
settings put secure enabled_accessibility_services "com.example/com.example.AccessibilityService"
settings put secure accessibility_enabled 1

# 启用非官方应用安装
settings put secure install_non_market_apps 1

# 启用开发者选项
settings put global development_settings_enabled 1

# 清空无障碍服务列表（急救）
settings put secure enabled_accessibility_services ""
```

## 5.4 授权时序经验

| 时序点 | 可靠性 | 说明 |
|--------|--------|------|
| `Activity.onCreate` | ❌ 不可靠 | Shizuku Binder 可能未就绪 |
| `Service.onStartCommand` | ✅ 可靠 | 服务连接时 Shizuku 已就绪 |
| 用户手动触发（按钮点击） | ✅ 最可靠 | 无需关心生命周期 |

---

# 六、模拟定位开发

## 6.1 核心原理

### 三源同时推送

模拟定位不能只改 GPS，很多 App 会同时参考多个位置源：

```kotlin
lm.addTestProvider("gps", false, true, false, true, true, true, true, 0, 5)
lm.setTestProviderEnabled("gps", true)
lm.addTestProvider("network", ...)
lm.addTestProvider("passive", ...)
```

### 持续推送

```kotlin
while (isActive) {
    val location = Location("gps").apply {
        latitude = curLat + jitterLat
        longitude = curLng + jitterLng
        accuracy = 8f + jitterRand.nextFloat() * 12f  // 8~20m 真实精度
        speed = 0.5f + jitterRand.nextFloat() * 1.5f
        time = System.currentTimeMillis()
    }
    lm.setTestProviderLocation("gps", location)
    delay(100)
}
```

## 6.2 参数优化

| 参数 | 错误值 | 正确值 | 原因 |
|------|--------|--------|------|
| accuracy | 1米 | 8~20米随机 | 真实 GPS 精度在 5~20 米 |
| speed | 0（恒定） | 0.5~2.0 随机 | 静止也有微小波动 |
| 抖动 | 无 | ±10米随机 | 模拟 GPS 漂移 |
| 推送频率 | 仅1次 | 每100ms持续 | App 需要持续更新的位置 |

## 6.3 Provider 注册并发问题

`addTestProvider()` / `removeTestProvider()` **不是线程安全的**。必须用 `synchronized` 锁保护：

```kotlin
synchronized(mockLock) {
    stopMockInternal()
    if (!registerProviders(m)) {
        Handler(mainLooper).postDelayed({
            synchronized(mockLock) { registerProviders(m); startPushLoop() }
        }, 1000)
        return
    }
    startPushLoop()
}
```

## 6.4 自愈推送循环

```kotlin
private fun startPushLoop() {
    thread(start = true) {
        // 第一阶段：快速 burst 推送 3 次
        repeat(3) { pushOnce(); Thread.sleep(150) }
        // 验证系统是否采纳
        if (!isMockActive()) {
            ShizukuShell.exec("appops set $packageName android:mock_location allow")
            registerProviders(m)
            repeat(5) { pushOnce(); Thread.sleep(150) }
        }
        // 第二阶段：正常轮询（Handler 异步，不阻塞主线程）
        handler.post(object : Runnable {
            override fun run() {
                pushOnce()
                handler.postDelayed(this, 200)
            }
        })
    }
}
```

---

# 七、GitHub 上传流程

## 7.1 核心教训

> **永远不要从 zip 备份上传！直接从源工作区复制**

- ❌ 从 sdcard 的 zip 解压后上传：备份可能不是最新版，可能缺少新增文件
- ✅ 从 Operit 工作区复制：`/data/user/0/com.ai.assistance.operit/files/workspace/{workspaceId}/`

## 7.2 完整流程

```bash
# 1. 清理构建产物
rm -rf .backup/ .operit/ app/build/ .gradle/ .idea/

# 2. 添加 .gitignore
echo ".gradle/
.idea/
build/
.local/
.backup/
.operit/
*.iml" > .gitignore

# 3. 初始化仓库
git init
git config user.name "your_username"
git config user.email "your_email@users.noreply.github.com"
git add .
git commit -m "描述信息"
git branch -M main

# 4. 推送
git remote add origin https://用户名:经典Token@github.com/用户名/仓库名.git
git push -f -u origin main
```

## 7.3 Token 说明

| Token 类型 | 用途 |
|-----------|------|
| **经典 Token（ghp_ 开头）** | ✅ 支持 git push |
| **细粒度 Token（github_pat_ 开头）** | ❌ 不支持 git push，仅用于 API |
| Token 权限 | 必须勾选 `repo` 全部权限 |

## 7.4 踩坑记录

### 仓库已存在时
- 不要创建新仓库，用 `git push -f` 强制覆盖
- 用 curl 先检查仓库是否存在

### 网络中断
- `Recv failure: Software caused connection abort` → 重试一次
- 检查 commit 中是否有 build/ 等大文件

### Authentication failed
- 检查 `git remote -v` 看 URL 是否正确
- 新 `git init` 后之前的 remote 配置丢失，需重新 `set-url`

### 带 tag 推送（回滚用）
```bash
git tag -a v1.0.0 -m "版本说明"
git push origin main --tags
```

回滚：`git checkout v1.0.0` 或 `git revert <commit-hash>`

---

# 八、UI自动化与AutoJS6

## 8.1 核心规则

> **进入目标 APP 界面后，所有操作通过已写好的 AutoJS6 脚本执行，以节省 token**

## 8.2 脚本体系

| 文件 | 功能 |
|------|------|
| 读取ui.js | 抓取当前屏幕所有可见控件(text, clickable, x, y)→输出 current_ui.json |
| 点击目标坐标.js | 读取目标坐标.txt → shizuku('input tap x y') 点击 |
| 目标坐标.txt | 存储点击坐标，格式 `x,y` |

## 8.3 运行命令

```bash
am start -n org.autojs.autojs6/org.autojs.autojs.external.open.RunIntentActivity \
    -a android.intent.action.VIEW \
    -d /storage/emulated/0/脚本/Operit/<脚本名.js> \
    -f 0x10000000
```

## 8.4 踩坑记录

| 坑点 | 说明 |
|------|------|
| ❌ ForegroundService 启动 | 后台无 toast，点击无效 |
| ✅ 必须用 RunIntentActivity 启动 | 正确方式 |
| ❌ click() 或 className().text().click() | 支付宝等有触摸防护 |
| ✅ 必须用 `shizuku('input tap x y')` | 绕过触摸防护 |

## 8.5 页面特征快速判断

| 页面 | 特征 |
|------|------|
| 首页 | "扫一扫""收付款""出行"等快捷卡片 |
| 搜索页 | "搜索历史"、搜索结果 |
| 我的页 | 用户昵称、手机号、"总资产" |
| 操作前先抓屏确认 | 每次点击后都运行读取ui.js |

---

# 九、Android 项目构建

## 9.1 环境版本

| 组件 | 版本 |
|------|------|
| Gradle | 8.9 / 9.1.0 |
| AGP | 8.7.3 / 9.0.0 |
| Kotlin | 2.0.21 / 2.3.10 |
| Java | 17 |
| Android SDK | 35 |
| UI 框架 | Jetpack Compose + Material3 |
| 构建环境 | Proot Ubuntu（移动端）/ Windows |

## 9.2 gradle.properties

```properties
org.gradle.jvmargs=-Xmx2048m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
```

## 9.3 国内镜像源

```kotlin
dependencyResolutionManagement {
    repositories {
        maven { setUrl("https://maven.aliyun.com/repository/public/") }
        maven { setUrl("https://maven.aliyun.com/repository/google/") }
        maven { setUrl("https://repo.huaweicloud.com/repository/maven/") }
        maven { setUrl("https://jitpack.io") }
        google()
        mavenCentral()
    }
}
```

## 9.4 AAPT2（Proot 环境）

```kotlin
android {
    aapt2 { useTarget(":linux-aarch64") }
}
// gradle.properties
android.aapt2.process.daemon=false
```

## 9.5 构建命令

```bash
# Debug
./gradlew assembleDebug

# Release（可能因 lint 报 ProtectedPermissions 失败）
./gradlew assembleRelease

# 带日志重定向（防止管道卡死）
./gradlew assembleDebug --no-daemon > /sdcard/Download/build_log.txt 2>&1

# 清理缓存后重试
rm -rf .gradle/ app/build/
```

## 9.6 踩坑速查表

| 错误 | 秒修 |
|------|------|
| `Cannot find implementation for ..._Impl` | 删 Room，换 Gson |
| `Not compatible with Kotlin build` | 别用 KAPT/KSP |
| `No value passed for parameter 'value'` | `MutableStateFlow<String?>()` |
| `collectLatest` 嵌套死锁 | 改成属性监听+手动过滤 |
| `INSTALL_FAILED_ABORTED` | 从 `/data/local/tmp/` 安装 |
| UP-TO-DATE（改了代码不重新编译） | `rm -rf app/build/` 再编 |
| `pm install -r` 后 App 被禁用 | `pm enable com.example.app` |

---

# 十、ActionPilot 项目架构

## 10.1 整体架构

```
┌─────────────────────────────────────┐
│          UI (Compose 全中文)          │
│   录制  │  列表  │  导出              │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│      RecordService（前台服务）        │
│  ┌─────────┐  ┌───────────────┐     │
│  │ Shizuku  │  │Accessibility  │     │
│  │ dumpsys  │  │  Service     │     │
│  │ 轮询     │  │ 点击事件+文字 │     │
│  └─────────┘  └───────┬───────┘     │
│                       ▼             │
│                 MapBuilder          │
│     节点合并+边去重+内容子页面+坐标    │
└──────────────────────┬──────────────┘
                       ▼
              MapRepository (Gson)
```

### 双通道录制

| 通道 | 数据 | 频率 |
|------|------|------|
| Shizuku dumpsys window | Activity 切换 | 500~1500ms |
| AccessibilityService | 点击/长按/输入 + 文字 | 事件驱动 |

## 10.2 数据模型

```
OpMap
├── nodes: Map<String, OpNode>   (页面节点 + 虚拟内容子节点)
│   ├── id: "package#screen" 或 "package#virtual:label"
│   ├── appPackage / appName / screenName
│   ├── firstSeen / lastSeen / visitCount
├── edges: List<OpEdge>          (页面跳转)
│   ├── fromId → toId
│   ├── actionType / elementLabel (触发动作)
│   ├── count / lastTime
└── actions: List<OpAction>      (点击级操作)
    ├── nodeId (发生在哪个页面)
    ├── actionType: CLICK / LONG_CLICK / TEXT_INPUT / TRANSITION / SCREEN_CONTENT
    ├── elementLabel (点击的文字 或 页面内容摘要)
    ├── viewId (viewId 或 "bounds:l,t,r,b | viewId" 带坐标)
    └── timestamp
```

### 虚拟内容子页面

同 Activity 内 H5 页面变化时，自动创建虚拟子节点：
- ID 格式：`package#virtual:页面标题`
- 首次内容不同时自动创建节点 + 关联边
- 同标题重复进入只增加 visitCount

## 10.3 关键文件

```
ActionPilot/src/main/java/com/operit/actionpilot/
├── model/
│   ├── OpNode.kt / OpEdge.kt / OpAction.kt / OpMap.kt
├── recorder/
│   └── MapBuilder.kt          (录制引擎 + onContentPage 虚拟节点)
├── service/
│   ├── RecordService.kt       (Shizuku 轮询 + appName 解析)
│   ├── RecordAccessibilityService.kt
│   │   ├── 点击/长按/输入捕获
│   │   ├── captureVisibleText() 页面内容遍历
│   │   ├── captureScreenContent() 内容子页面
│   │   └── extractClickInfo() 带 bounds 坐标
│   └── ShizukuShell.java      (反射 newProcess)
├── storage/
│   └── MapRepository.kt       (Gson 持久化)
├── export/
│   └── AiExporter.kt          (AI 导出 JSON)
└── ui/
    ├── MainScreen.kt          (3 Tab: 录制/列表/导出)
    ├── RecordListScreen.kt    (操作列表)
    └── ExportScreen.kt        (导出)
```

## 10.4 版本历史

| 版本 | 日期 | 改动 |
|------|------|------|
| v1.0.0 | 05-07 | Shizuku dumpsys 轮询录制 |
| v1.1.0 | 05-08 | 无障碍录制 + 点击文字捕获 + UI 中文 |
| v1.1.1 | 05-08 | 加开发经验宝典 |
| v1.2.0 | 05-08 | 应用选择列表 + 录制过滤 |
| v1.3.0 | 05-08 | 全量录制 + 页面内容捕获 + 虚拟节点 + 点击坐标 + 删除应用过滤 |

---

# 十一、万能APP疑难杂症

## 11.1 闪退：括号不匹配

**现象**：`apply_file` 导致花括号数量不等（`{52个}53个`），函数嵌套错位。

**解决**：`grep -o '{' file | wc -l` 和 `grep -o '}' file | wc -l` 定期检查匹配。

## 11.2 坐标从未推送到系统

**根因**：`registerProviders` 成功后没有 `return`，递归不停止。

## 11.3 主线程 sleep 阻塞

**根因**：`Thread.sleep` 阻塞 Handler 线程，导致 UI 更新延迟。

**修复**：改为 burstCount 连推模式（第一次推送 3 次，然后切换 Handler 异步轮询）。

## 11.4 第一次启动定位延迟

**根因**：Handler 第一轮 post 失败无重试机制。

**修复**：burst 推送 3 次 + 验证是否采纳 → 不采纳则重新授权 + 再推 5 次。

## 11.5 自动授权时序

**根因**：`AdbCommandActivity.onCreate` 中执行 Shizuku 命令，此时 Shizuku Binder 未就绪。

**修复**：移到 `MockLocationService.onStartCommand` 中执行。

## 11.6 编译 UP-TO-DATE

**现象**：在 `/tmp/` 修改代码后，Gradle 检测不到源码变化。

**解决**：先 `rm -rf /tmp/project` 再重新 `cp` 编译。

---

# 十二、通用经验教训

## 12.1 核心原则

> **代码结构比逻辑更重要**：括号不匹配、函数嵌套错了，逻辑再对也跑不起来

1. **先查资料再动手** — 出问题先看 logcat 确认异常类型 → 搜解决方案 → 确认 root cause 后动手
2. **不要闭门造车** — 你在会遇到的问题，别人一定已经遇到并解决了
3. **每一步都验证** — 改一行代码编译一次，不等到最后一次性验证

## 12.2 代码检查清单

- [ ] 反射调用参数传递方式是否正确（Method.invoke 可变参数）
- [ ] 异常不要完全吞掉，最少输出到 logcat
- [ ] 递归函数是否有明确的终止条件
- [ ] 花括号数量是否匹配：`grep -o '{' file | wc -l` = `grep -o '}' file | wc -l`
- [ ] 函数嵌套是否正确：`grep -n "private fun\|fun "` 检查函数定义位置
- [ ] `apply_file` / `Edit` 大段替换是否漏掉或破坏了相邻代码

## 12.3 调试命令

```bash
# 崩溃日志
adb logcat -b crash -d

# 指定 Tag
adb logcat -s "ActionPilot" -d

# 模拟定位状态
dumpsys location | grep -A5 "gps provider \[mock\]"

# 无障碍服务配置
settings list secure | grep accessibility

# 当前焦点窗口
dumpsys window | grep mCurrentFocus

# 包信息
dumpsys package <pkg>
```

## 12.4 Shizuku 专区

1. 反射调用 newProcess 必须放 Java 文件（Kotlin 编译检查拦截 private 方法）
2. `AppOpsService.setMode()` 权限不够，用 shell 命令替代
3. 不要搞 Binder transact 或 UserService，shell 命令就是最直接的方式
4. Shizuku 绑定时序：**Service.onStartCommand 比 Activity.onCreate 更可靠**
5. 异常不要完全吞掉，最少输出到 logcat

## 12.5 文件与存储

1. 文件写入必须用 `filesDir`（内部存储），写 `/sdcard/` 在 Android 10+ 可能被 SELinux 拦截
2. 先写缓存文件再 rename，防止写坏
3. APK 从 `/sdcard/` 安装被 SELinux 限制 → 从 `/data/local/tmp/` 安装

## 12.6 无障碍服务安全

1. **全部功能用户手动触发，绝不在 onServiceConnected 中自动启动**
2. **用 START_NOT_STICKY**，防止被杀后自动重启
3. **监听 TYPE_WINDOW_CONTENT_CHANGED 的安全做法**：必须加限流 + 内容去重，不在高频回调中做耗时操作（遍历 UI 树）。ActionPilot 的做法：1s 限流 + content hash 去重，每次只捕获可见文本，不做悬浮窗覆盖。
4. **递归遍历树**：深度限制 ≤ 25 层，节点及时 recycle
5. 开发时连好 ADB，写好卸载脚本，备用机测试

---

---

# 十三、AI 工具架构（来自 Operit）

## 13.1 核心套路

**AI 不构造命令，AI 只选工具名 + 填参数。** 命令在 app 代码里写死、验证过。

```
用户说"定个12点闹钟"
  ↓
AI 看到工具清单里有：
  set_alarm(hour, minutes) — "设置闹钟"
  search_web(query) — "联网搜索"
  ...
  ↓
AI 输出：{"tool": "set_alarm", "params": {"hour": 12, "minutes": 0}}
  ↓
App 执行写死的代码：
  am start -a android.intent.action.SET_ALARM --ei HOUR 12 --ei MINUTES 0
  ↓
结果喂回 AI
```

## 13.2 三要素

1. **工具库** — 几十个写好的功能代码，每个对应真实 Android 操作
2. **工具说明书（Manifest）** — 每次请求把工具清单（name/description/parameters）塞进上下文
3. **提示策略** — "优先用工具解决问题，别自己瞎编"

## 13.3 工具定义格式

每个工具需要：
- `name`: 工具名（AI 用这个调用）
- `description`: 一句话说清干什么
- `parameters`: 参数列表（name/type/required/description）

**设计原则**：粒度适中、每个工具一个职责、参数尽量少、失败有回退、工具名用英文下划线。

## 13.4 Operit 工具包分类

| 包 | 工具数 | 功能 |
|-----|--------|------|
| system_tools | 13 | 系统设置、应用管理、广播、Intent、设备信息 |
| automatic_ui_base | 11 | 点击、滑动、输入、截图、UI 树 |
| browser | 18 | 类 Playwright 浏览器自动化 |
| various_search | 8 | Bing/百度/搜狗/夸克 多引擎搜索 |
| extended_http_tools | 3 | HTTP 请求、文件上传、Cookie 管理 |
| extended_file_tools | 8 | 文件复制/移动/压缩/解压/分享 |

---

# 十四、Shizuku AI 开发经验

## 14.1 搜索

### OkHttp vs HttpURLConnection

**HttpURLConnection** 是 Java 内置的，但在 Android 上表现不稳定：SSL 处理差、重定向有时不跟、超时表现不一致。**OkHttp** 是 Android 生态的事实标准，Operit 也用。

```java
// OkHttp 三引擎搜索
implementation "com.squareup.okhttp3:okhttp:4.12.0"
```

### 三引擎搜索策略

```java
String result = searchDuckDuckGo(keyword);  // HTML duckduckgo.com
if (result == null) result = searchBing(keyword);     // bing.com
if (result == null) result = searchBaidu(keyword);    // baidu.com
```

- DuckDuckGo HTML（`html.duckduckgo.com`）结构稳定，class 名 `result__a` / `result__snippet`
- Bing 用 `b_algo` class，注意找 h2 > a
- Baidu 用 `c-container` class，注意 div 嵌套匹配

### User-Agent 很重要

```java
// Windows Chrome UA 比 Android UA 更容易得到完整页面
"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/122.0.0.0 Safari/537.36"
```

### 屏蔽搜索结果域名

```java
private static final String[] BLOCKED_DOMAINS = {
    "zhihu.com",  // 知乎在手机上只显示二维码
    // 可以继续加 csdn.net, baidu.com 等
};
```

### 自动读页面 + 自动关浏览器

浏览器加载完成后，延迟 1.5 秒自动执行 `document.body.innerText` 提取页面文字，提交回 AI 分析。读完自动关闭浏览器浮层。

```java
webView.setWebViewClient(new WebViewClient() {
    @Override
    public void onPageFinished(WebView view, String url) {
        new Handler().postDelayed(() -> readCurrentPage(), 1500);
    }
});
```

## 14.2 悬浮窗键盘焦点（TYPE_APPLICATION_OVERLAY）

### 问题

`TYPE_APPLICATION_OVERLAY` 悬浮窗打开时，微信等后台应用点击输入框无法弹出键盘。浮窗输入框和后台 App 输入框之间无法自由切换焦点。

### 根因

- `FLAG_NOT_FOCUSABLE` 让浮窗永远不聚焦 → 浮窗自己弹不出键盘
- 不加 `FLAG_NOT_FOCUSABLE` → 浮窗抢焦点不放手 → 后台 App 拿不到焦点
- `FLAG_ALT_FOCUSABLE_IM` → 浮窗自身弹不出键盘（与直觉相反）
- 传统 EditText 的 `requestFocus()` 行为可能和 Jetpack Compose 的 `FocusRequester` 不同

### 当前实现（FloatService.java L238-L387，Operit 完整方案，问题未完全解决）

```java
// ====== 1. 全屏透明遮罩，放在浮窗下层，捕获外部点击释放焦点 ======
private void ensureFocusDismissView() {
    focusDismissView = new View(this);
    focusDismissView.setClickable(true);
    focusDismissView.setOnTouchListener((view, event) -> {
        if (event.getAction() == MotionEvent.ACTION_DOWN) { hideKeyboard(); }
        return true;
    });
    WindowManager.LayoutParams dp = new WindowManager.LayoutParams(
        MATCH_PARENT, MATCH_PARENT,
        TYPE_APPLICATION_OVERLAY,
        FLAG_NOT_FOCUSABLE,
        PixelFormat.TRANSLUCENT
    );
    wm.addView(focusDismissView, dp);
}

// ====== 2. 显示键盘：去掉 NOT_FOCUSABLE + 启用遮罩 ======
private void showKeyboard(EditText et) {
    p.flags &= ~FLAG_NOT_FOCUSABLE;
    p.flags |= FLAG_NOT_TOUCH_MODAL | FLAG_WATCH_OUTSIDE_TOUCH;
    wm.updateViewLayout(v, p);
    ensureFocusDismissView();
    setFocusDismissOverlayEnabled(true);
    scheduleImeShow(et, 0, 200);
}

// ====== 3. 带重试的 IME 调度 ======
private void scheduleImeShow(EditText et, int retryCount, long delayMs) {
    focusHandler.postDelayed(() -> {
        if (!v.isAttachedToWindow() || v.getWindowToken() == null) {
            if (retryCount < 4) scheduleImeShow(et, retryCount + 1, 50);
            return;
        }
        et.requestFocus();
        View focused = v.findFocus();
        if (focused == null || !focused.isAttachedToWindow()) {
            if (retryCount < 4) scheduleImeShow(et, retryCount + 1, 50);
            return;
        }
        InputMethodManager imm = (InputMethodManager) getSystemService(INPUT_METHOD_SERVICE);
        imm.showSoftInput(focused, SHOW_IMPLICIT);
    }, delayMs);
}

// ====== 4. 隐藏键盘：清焦点 + 恢复 NOT_FOCUSABLE ======
private void hideKeyboard() {
    focusHandler.removeCallbacks(pendingImeFocusRunnable);
    pendingImeFocusRunnable = null;
    InputMethodManager imm = (InputMethodManager) getSystemService(INPUT_METHOD_SERVICE);
    View focused = v.findFocus();
    if (focused != null) focused.clearFocus();
    v.clearFocus();
    imm.hideSoftInputFromWindow(v.getWindowToken(), 0);
    p.flags |= FLAG_NOT_FOCUSABLE;
    p.flags &= ~(FLAG_NOT_TOUCH_MODAL | FLAG_WATCH_OUTSIDE_TOUCH);
    wm.updateViewLayout(v, p);
    setFocusDismissOverlayEnabled(false);
}
```

### Operit 方案 vs Shizuku AI 的关键差异

两个项目都用 `TYPE_APPLICATION_OVERLAY` + 同样的焦点管理策略，但 Operit 丝滑切换而 Shizuku AI 失败：

| 维度 | Operit | Shizuku AI |
|------|--------|------------|
| UI 框架 | Jetpack Compose（ComposeView） | 传统 XML + EditText |
| 焦点机制 | `FocusRequester` + `Modifier.onFocusChanged` | `EditText.requestFocus()` |
| 其他服务干扰 | 纯浮窗 | 同时运行 AccessibilityService + AI 回调 |

**怀疑方向**（未解决）：
1. AccessibilityService 的 `TYPE_WINDOW_STATE_CHANGED` 事件可能抢焦点
2. AI 回调频繁 `Handler.post` 到主线程，打断焦点切换
3. Compose 的 `FocusRequester` 和传统 EditText 的 `requestFocus()` 行为不同
4. 可能需要更长延迟（500ms+）等 UI 更新完成后再请求焦点

## 14.3 AI 响应解析

### 扫全文找 JSON

AI 可能会先说一句话再输出工具调用 JSON。不能只检查首字符是不是 `{`，要扫全文找 `{"tool"`：

```java
int toolJsonStart = trimmed.indexOf("{\"tool\"");
if (toolJsonStart >= 0) {
    // 文字在前，JSON 在后，分别处理
}
```

### 工具调用循环

AI 输出 JSON → app 解析 → 执行工具 → 结果喂回 AI → AI 决定下一步或回复用户。不是一次性的。

## 14.4 常用 Intent

```bash
# 设置闹钟（标准 Android API，支持 SKIP_UI 跳过确认）
am start -a android.intent.action.SET_ALARM --ei android.intent.extra.alarm.HOUR 12 --ei android.intent.extra.alarm.MINUTES 0 --ez android.intent.extra.alarm.SKIP_UI true
# 查看闹钟列表
am start -a android.intent.action.SHOW_ALARMS

# 原子笔记（vivo）创建笔记（打开编辑界面预填内容，需手动保存）
am start -a android.intent.action.SEND -t text/plain --es android.intent.extra.TEXT "笔记内容" -p com.android.notes
# 带标题
am start -a android.intent.action.SEND -t text/plain --es android.intent.extra.TEXT "内容" --es android.intent.extra.SUBJECT "标题" -p com.android.notes
# 注意：vivo.intent.action.INSERT_NEW_NOTE 和 CREATE_NEW_NOTE 不可靠

# vivo 手电筒（settings put system，不是 secure！）
settings put system FlashState 1
settings put system back_flashlight_state 1
settings put system FlashState 0
settings put system back_flashlight_state 0
```

## 14.5 打包安装

```bash
export JAVA_HOME="C:/Users/ql/AppData/Local/Temp/jdk-17.0.14+7"
export ANDROID_HOME="C:/Users/ql/AppData/Local/Android/Sdk"
export MSYS_NO_PATHCONV=1
cd "F:/app/Claude/app/shizuku-ai"
./gradlew assembleDebug
adb -s 10CE2S1FPD0027X push app/build/outputs/apk/debug/app-debug.apk /data/local/tmp/shizuku-ai.apk
adb -s 10CE2S1FPD0027X shell pm install -r /data/local/tmp/shizuku-ai.apk
```

### 踩坑记录

**1. local.properties SDK 路径错误**
从 Proot Ubuntu 复制的项目，sdk.dir = `/root/Android`（Linux 路径）。
```properties
sdk.dir=C\:\\Users\\ql\\AppData\\Local\\Android\\Sdk
```
或用环境变量 `ANDROID_HOME=C:/Users/ql/AppData/Local/Android/Sdk`

**2. gradle-wrapper.jar 缺失**
独立项目没有 wrapper jar 和 properties，从缓存复制：
```bash
cp /c/Users/ql/.gradle/caches/<hash>/gradle-8.9/gradle/wrapper/gradle-wrapper.jar ./gradle/wrapper/
```

**3. AGP / Gradle 版本不匹配**
AGP 9.0.0 要求 Gradle 9.1+，但缓存只有 8.9。
降 AGP 到 8.7.3 可配合 Gradle 8.9 编译。

**4. Gradle JVM 被 kill（exit 137）**
gradle.properties 里 `org.gradle.jvmargs=-Xmx2048m` 需要 2GB。
如果机器内存不足，先 `./gradlew --stop` 杀残留 daemon，或改小内存。

**5. 签名不一致安装失败**
```
INSTALL_FAILED_UPDATE_INCOMPATIBLE: Existing package signatures do not match
```
先卸载旧版再安装：
```bash
adb uninstall com.shizuku.ai
adb shell pm install -r /data/local/tmp/shizuku-ai.apk
```

## 14.5 编译踩坑：JDK 问题

### 1. jlink 裁剪版 JDK 缺少 jvm.cfg

从 `aka.ms/download-jdk/microsoft-jdk-17.0.14-windows-x64.zip` 下载的 JDK 如果是 jlink 裁剪版，
`lib/` 下只有 `modules` 文件，没有 `jvm.cfg` 和 `java.security`。

运行 `java -version` 报错：
```
Error: could not open `C:\...\jdk-17.0.14+7\lib\jvm.cfg'
```
运行 Gradle 报错：
```
java.lang.InternalError: Error loading java.security file
```

**解法 1**：手动创建 jvm.cfg（临时救急）
```bash
echo "-server KNOWN" > "$JAVA_HOME/lib/jvm.cfg"
```
但 Gradle 仍可能因为缺少 `java.security` 或安全 provider 失败，
这种 JDK 只适合跑特定应用，不适合做 Android 构建。

**解法 2**（推荐）：用 Android Studio 自带的 JBR（JetBrains Runtime）
```bash
export JAVA_HOME="E:/Program Files/Android/Android Studio/jbr"
```
JBR 是完整 JDK，包含所有安全配置，AGP 8.7.3 + JDK 21 实测可正常编译。

### 2. JAVA_HOME 路径有空格 / 特殊字符

在 bash (MSYS2) 中需要用双引号括起来：
```bash
export JAVA_HOME="E:/Program Files/Android/Android Studio/jbr"
export ANDROID_HOME="C:/Users/ql/AppData/Local/Android/Sdk"
export MSYS_NO_PATHCONV=1  # 防止 MSYS2 把 /xxx 转成 Windows 路径
```

### 3. JDK 版本要求

- AGP 8.7.3 → JDK 17+
- JDK 21 (JBR) 兼容 AGP 8.7.3
- JDK 17 (Microsoft) 也可用，但必须下载完整版（非 jlink 裁剪版）

## 14.6 核心原则：所有功能必须工具化

**AI 不猜命令，AI 只选工具名+填参数**。这是确保 AI 行为准确的核心原则。

```
用户说"读取我最近的操作记录"
  ↓
AI 看到工具清单里有 read_uimap
  ↓
AI 输出：{"tool": "read_uimap", "params": {}}
  ↓
App 执行写死的代码（UiMapRecorder.exportForAI()）
  ↓
结果喂回 AI："你最近打开了微信→支付→收付款，点击了3次..."
```

每个能力都要对应一个工具，不要依赖 AI 自己去拼凑 shell 命令。

## 14.7 UI 操作录制（UiMapRecorder + read_uimap）

### 架构
从 ActionPilot 的 MapBuilder 移植精简版，2026-05-11 整合进 Shizuku AI。

```
SiriBall 显示 → UiMapRecorder.start()
   ├─ A11y 事件 → onWindowChanged / onAction(CLICK) / onScreenContent
   └─ Shizuku dumpsys 轮询（1.5s）→ fallback

AI 调 read_uimap → exportForAI() → JSON (apps/screens/flows/timeline)

关闭/销毁 → stop() → 持久化到 uimap.json（累积更新永不清零）
```

### 工具
- **read_uimap**（第13个工具）
- **create_note**（第14个工具）— 用 SEND Intent 打开原子笔记编辑界面预填内容

### 关键文件
```
shizuku-ai/app/src/main/java/com/shizuku/ai/
├── UiMapRecorder.java       # 录制引擎 + 数据模型 + 持久化 + AI 导出
├── ShizukuAccessibilityService.kt  # 加 uiMapRecorder 静态引用，事件透传
├── FloatService.java        # 启停录制 + read_uimap/create_note 工具分发
└── AIAgent.java             # 工具清单 + 工具说明书
```

### 当前 Shizuku AI 完整工具清单（14个）

| # | 工具名 | 功能 |
|---|--------|------|
| 1 | search_web | 联网搜索 |
| 2 | browse_url | 浏览器打开网页 |
| 3 | read_page | 读取浏览器页面内容 |
| 4 | read_screen | 无障碍读屏幕（新增强制刷新） |
| 5 | start_app | 打开应用 |
| 6 | list_apps | 列出安装应用（含[系统]标记，全量）|
| 7 | toggle_flashlight | 手电筒开关 |
| 8 | set_alarm | 设置闹钟（SKIP_UI vivo跳过确认）|
| 9 | execute_shell | 执行 Shell 命令 |
| 10 | execute_intent | 执行 Android Intent |
| 11 | learn | 记住经验 |
| 12 | get_device_info | 获取设备信息 |
| 13 | read_uimap | 读取 UI 操作地图 |
| 14 | create_note | 创建原子笔记（SEND Intent 预填）|

## 14.8 关键修复：AccessibilityWindowInfo 替代 rootInActiveWindow

### 问题
当 TYPE_APPLICATION_OVERLAY 悬浮窗显示时，`rootInActiveWindow` 可能返回空或悬浮窗自身内容，导致 `read_screen` 读不到底层桌面的图标和应用名。

### 解决方案
用 `getWindows()` 遍历所有窗口，过滤掉自己的浮窗，读取其他窗口内容：

```kotlin
val windows = try { getWindows() } catch (_: Exception) { null }
if (windows != null) {
    for (window in windows) {
        val pkg = window.root?.packageName?.toString() ?: ""
        if (pkg == "com.shizuku.ai") continue  // 跳过自己的浮窗
        if (window.type == AccessibilityWindowInfo.TYPE_ACCESSIBILITY_OVERLAY) continue
        val root = window.root ?: continue
        try { collectVisibleText(root, allTexts, 0) } finally { root.recycle() }
        window.recycle()
    }
}
```

### 要点
- `getWindows()` 返回所有可见窗口（API 21+），比 `rootInActiveWindow` 可靠
- 需要 import `android.view.accessibility.AccessibilityWindowInfo`
- 每个 `AccessibilityWindowInfo` 和 `AccessibilityNodeInfo` 都要 `recycle()`
- 兜底：`getWindows()` 失败时回退到 `rootInActiveWindow`

## 14.9 关键修复：AI 输出格式化 JSON 解析失败

### 问题
DeepSeek 等 LLM 输出工具调用时可能带格式化缩进：
```json
{
  "tool": "read_screen",
  "params": {}
}
```
但 `AIAgent.parseResponse()` 用 `indexOf("{\"tool\"")` 搜索 `{"tool"`（花括号紧贴双引号），格式化 JSON 的换行导致匹配失败。

### 三个连续坑

1. **`indexOf` 不支持空格** — `trim` 后是 `{\\n  "tool"`，不是 `{"tool"`
2. **`toolJsonStart > 0` 边界** — 当 JSON 是 AI 唯一输出时 `toolJsonStart = 0`，条件不满足被跳过
3. **`startsWith` 二次检查** — 找到起始位置后还有 `jsonPart.startsWith("{\"tool\"")` 二次检查，同样不支持空格

### 修复方案
```java
// 正则匹配（支持 { 和 "tool" 之间任意空白）
int toolJsonStart = trimmed.indexOf("{\"tool\"");
if (toolJsonStart < 0) {
    java.util.regex.Matcher m = java.util.regex.Pattern.compile(
        "\\{\\s*\"tool\"").matcher(trimmed);
    if (m.find()) toolJsonStart = m.start();
}

// >= 0 而非 > 0
if (toolJsonStart >= 0) {
    jsonPart = trimmed.substring(toolJsonStart).trim();
    // 直接 JSONObject 解析（原生支持空格），不用 startsWith 二次检查
    JSONObject toolCall = new JSONObject(jsonPart);
    ...
}
```

### 教训
- AI 输出的 JSON 不要假设紧凑格式，一定要处理空白符
- `JSONObject`/`Gson` 自带空格处理，不要自己用 `indexOf`/`startsWith` 校验
- 边界值处理：`> 0` 和 `>= 0` 的区别在 JSON 唯一输出时致命

## 14.10 read_screen 增强：始终返回控件结构+坐标

### 问题
`captureDetailedScreen()` 在 `captureScreen()` 读到文字后提前返回，不收集控件结构。AI 只有文字没有坐标，无法定位控件。

### 修复
- 去掉 `if (lastScreenText.isNotEmpty()) return`，始终收集结构
- `saveStructure()` 不再伪造 `lastScreenText`
- `readScreenContent()` 合并输出：文字 + 控件列表（class / label / bounds / clickable）
- `captureScreen()` 捕获为空时清除 `lastScreenText = ""` 防脏读
- 去掉无用的 dumpsys 兜底（vivo dumpsys 读不到 UI 信息）

### accessibility_service_config.xml
必须添加 XML 配置文件和 manifest meta-data，仅靠代码 `serviceInfo` 设置可能不够：
```xml
<!-- res/xml/accessibility_service_config.xml -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100" />
```
```xml
<!-- AndroidManifest.xml service 声明内 -->
<meta-data
    android:name="android.accessibilityservice"
    android:resource="@xml/accessibility_service_config" />
```
